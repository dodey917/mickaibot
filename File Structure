your-bot-folder/
├── .gitignore     # Lists files to ignore (contains ".env")
├── bot.py          # Main bot code
├── requirements.txt
└── .env            # LOCAL ONLY - contains secrets (not committed)
